#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pa11y-ci-reporter-html/bin/pa11y-ci-reporter-html.js" "$@"
  ret=$?
else 
  node  "$basedir/../pa11y-ci-reporter-html/bin/pa11y-ci-reporter-html.js" "$@"
  ret=$?
fi
exit $ret
